import 'package:cadastro_app/views/CadastroView.dart';
import 'package:flutter/material.dart';

class PessoaController extends StatelessWidget {
  const PessoaController({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: const CadastroView());
  }
}
